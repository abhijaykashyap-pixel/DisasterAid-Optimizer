# DisasterAid-Optimizer
A data-driven disaster relief allocation tool — clusters countries by vulnerability using K-Means + PCA, then computes risk-weighted fund distribution to support equitable humanitarian decision-making.
